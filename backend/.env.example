# Application
PORT=
NODE_ENV="development"
PREFIX="api/v1/"
ALLOWED_ORIGINS=["",""] # Place origins from we accept api requests
CLIENT_ORIGIN=
JWT_SECRET_KEY=


# Database (Note: Database will be always on the local system not in docker container as docker container runs on ram not rom)
DATABASE_HOST="localhost" # for now our host will be local system
DATABASE_PORT=5432
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_NAME=

# Swagger Configuration
SWAGGER_TITLE=""
SWAGGER_DESCRIPTION=""
SWAGGER_VERSION="1.0"
SWAGGER_URL="api-docs"



